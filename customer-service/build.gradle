plugins {
    id 'java'
}

group 'demo'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

/*configurations {
    all*.exclude module: 'javax.servlet:servlet-api:2.5'
}*/

dependencies {
    implementation group: 'org.springframework.boot', name: 'spring-boot-starter-web', version: "${spring_boot_version}"
    implementation group: 'org.springframework.boot', name: 'spring-boot-starter-actuator', version: "${spring_boot_version}"
    implementation group: 'org.springframework.cloud', name: 'spring-cloud-starter-netflix-eureka-client', version: '2.1.6.RELEASE' //'Finchley.RELEASE'
    implementation group: 'com.google.code.gson', name: 'gson', version: '2.8.6'

/*    compile('org.springframework.cloud:spring-cloud-starter-netflix-eureka-client:3.1.0') {
        exclude module: 'javax.servlet:javax.servlet-api:2.5'
    }*/
    //testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    //testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
}

test {
    //useJUnitPlatform()
}


